export const selectIsLoading = (state) => state.product.isLoading;

export const selectProducts = (state) => state.product.products;

export const selectOneMedicine = (state) => state.product.oneMedicine;
